import React from "react";

export const ExerciseList: React.FunctionComponent = () => {

    // useEffect(() => {
    // //     Add in logic to check for exercises that have been added and render loading/empty state if none found
    // }, [])

    return (
        <section style={{border: "solid 1px", margin: "0rem 2.5rem"}}>
            <h2>Available Exercises</h2>
            {/* this is going to be a scrolling list of exercises that have already been added*/}
        </section>
    )
}